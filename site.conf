Listen 80
Listen 443
<VirtualHost *:80>
        ServerAdmin $ADMINMAIL
        ServerName $SNAME
        ServerAlias $SALIAS
        DocumentRoot /var/www/$SITE/htdocs
        ErrorLog ${APACHE_LOG_DIR}/error.log
        ProxyPreserveHost On
</VirtualHost>
<VirtualHost *:443>
        SSLEngine on
        SSLCertificateKeyFile /etc/ssl/certs/dispositivo.key
        SSLCertificateFile /etc/ssl/certs/device.crt
        SSLCACertificateFile /etc/ssl/certs/cacert.pem
        ProxyPass /compras https://$IPCOMPRAS:443/
        ProxyPassReverse /compras https://$IPCOMPRAS:443/
</VirtualHost>
